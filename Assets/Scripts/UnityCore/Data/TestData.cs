using System.Collections;
using System.Collections.Generic;
using UnityEngine.InputSystem;
using UnityEngine;

//Renaissance Coders
//How To Code Data Storage In Unity3D, Game Essentials (2/5)
//https://www.youtube.com/watch?v=Vhuf1e0PVH0

namespace UnityCore
{
    namespace Data
    {
        public class TestData : MonoBehaviour
        {
            /*public DataController dataController;

            #region Unity Functions
            #if UNITY_EDITOR
            private void Update()
            {
                if (Input.GetKeyUp(KeyCode.P))
                {
                    TestAddScore(1);
                    Log("Score = " + dataController.Score + "| Highscore = " + dataController.HighScore);
                }

                 if (Input.GetKeyUp(KeyCode.O))
                {
                    TestAddScore(-1);
                    Log("Score = " + dataController.Score + "| Highscore = " + dataController.HighScore);
                }

                 if (Input.GetKeyUp(KeyCode.V))
                {
                    TestResetScore();
                    Log("Score = " + dataController.Score + "| Highscore = " + dataController.HighScore);
                }

            }

            #endif
            #endregion

            #region Private Functions

            private void TestAddScore(int _delta)
            {
                dataController.Score += _delta;
            }

            private void TestResetScore(int _delta)
            {
                dataController.Score = 0;
            }

            private void Log(string _msg) 
            {
                Debug.Log("[Test Data]" + _msg);
            }

            #endregion*/
        }
    }
}