using UnityEngine.Audio;
using UnityEngine;

[System.Serializable]
public class SoundManager
{
    /*public string name;
    public AudioClip clip;

    [Rang(0f, 1f)]
    public float volume;
    [Rane(0.1f, 3f)]
    public float pitch;

    private static Dictionary<Sound, float> soundTimerDictionary;

    private static GameObject oneShotGameObject;
    private static AudioSource oneShotAudioSource;

    private AudioSource[] mySounds;

    private AudioSource organCmajor;
    private AudioSource trumpetCmajor;
    private AudioSource tomDrum;
    private AudioSource elecGuitar;
    private AudioSource shaker;

    // Start is called before the first frame update
    void Start()
    {
        mySounds = GetComponents<AudioSource>();
        organCmajor = mySounds(0);

    }

    public enum Sound
    {
        //List all sounds here
        organCmajor,
        trumpetCmajor,
        tomDrum,
        elecGuitar,
        shaker,
        
    }

    public void PlayOrganCmajor() 
    {
        organCmajor.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
